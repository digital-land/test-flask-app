Get started

```
pip install -r requirements.txt
npm install
```

You might need to add the extra npm dependencies.

```
"copyfiles": "latest",
"govuk-frontend": "^3.14.0",
"node-sass": "^6.0.1",
"nps": "^5.10.0"
```
